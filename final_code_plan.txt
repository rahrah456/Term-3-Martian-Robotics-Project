Core 2 of the CPU will constantly read sensor values so we always have the most up to date values. and now

Core 1 of the CPU will handle everything else. It must use non-blocking code(?)
It will store a hardcoded version of the map.
The control flow will consist of locating ourselves and mapping other robots.
We will use primarily the motor encoders and the IMU to locate our ourself. When available we will use these to help locate as well: RFID reader, the IR sensor on lines, UDS on walls.
We will use the UDS to map other robots.
Then we have memory system based on the certainty we originally had and how long its been since we took the measurement for where other robots are and perhaps where we are.
We will then decide on a path to go to deposit seeds.
Then we deposit seeds by locating the hole with first RFID then IR. We use the IR to detect seed presence. and locate the centre of the hole. If unoccupied and the server says unoccupied, then deposit a seed.
This core(?) also handles communication with the server.



There will be a dashboard which sends all the data to a laptop over MQTT so we can visualise the map and what the robot sees. We can also send commands back to tune the PIDs and run unit tests.q1
